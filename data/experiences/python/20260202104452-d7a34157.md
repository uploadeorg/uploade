# Use async for file operations in high-load apps

Category: python
Type: tip
Tags: async, files, optimization

When handling many concurrent file operations, use aiofiles instead of standard open(). This prevents blocking the event loop and improves throughput significantly.